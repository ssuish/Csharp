class DataTypes
{
    // Value Types - directly contain data. 
    // Boolean Value : true/false
    bool defBool = false;

    // 8-bit unsigned integer : 0 - 255
    byte defByte = 0;
    byte minByte = byte.MinValue;
    byte maxByte = byte.MaxValue;

    // 16-bit unicode char : U+0000 to U+ffff
    char defChar0 = '\0';
    char maxChar = char.MaxValue;
    char minChar = char.MinValue;

    // 128-bit precise decimal values 
    decimal defDec0 = 0.0M;
    decimal minDec = decimal.MinValue;
    decimal maxDec = decimal.MaxValue;

    // 64-bit double precision floating point 
    double defDouble0 = 0;
    double defDouble1 = 0.0D; // Alternate
    double minDouble = double.MinValue;
    double maxDouble = double.MaxValue;

    // 32-bit single precision floating point
    float defFloat0 = 0;
    float defFloat1 = 0.0F; // Alternative
    float maxFloat = float.MaxValue;
    float minFloat = float.MinValue;

    // 32-bit signed integer
    int defInt = 0;
    int maxInt = int.MaxValue;
    int minInt = int.MinValue;

    // 64-bit signed integer
    long defLong0 = 0;
    long defLong1 = 0L;
    long maxLong = long.MaxValue;
    long minLong = long.MinValue;

    // 16-bit signed integer
    short defShort = 0;
    short maxShort = short.MaxValue;
    short minShort = short.MinValue;

    // 8-bit signed integer
    sbyte defSbyte = 0;
    sbyte maxSbyte = sbyte.MaxValue;
    sbyte minSbyte = sbyte.MinValue;

    // 32-bit unsigned integer
    uint defUint = 0;
    uint maxUint = uint.MaxValue;
    uint minUint = uint.MinValue;

    // 64-bit unsigned integer
    ulong defUlong = 0;
    ulong maxUlong = ulong.MaxValue;
    ulong minUlong = ulong.MinValue;

    // 16-bit unsigned integer
    ushort defUshort = 0;
    ushort maxUshort = ushort.MaxValue;
    ushort minUshort = ushort.MinValue;
    
    // ================================

    // Reference Type
    

}